# Bytefall GRUB Theme

This directory contains the text theme source. Release packaging should add the PNG frame assets referenced by `theme.txt` and install the directory to:

```text
/boot/grub/themes/bytefall
```

Then set:

```text
GRUB_THEME="/boot/grub/themes/bytefall/theme.txt"
```

